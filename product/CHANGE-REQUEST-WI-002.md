# Change Request WI-002 — Cross-Time Signature + Agent Skill Packaging

Date: 2026-09-08
Source: `product/WINNER-INTELLIGENCE-RERUN-2026-09-08.md`
Owner for implementation: PBPD
Change authority basis: existing human continuation authorization in `product/AUTHORIZATION.md`
Scope class: BOUNDED_POSITIONING_AND_INTEGRATION_REWORK

## Why this change exists

Current same-event research found material collision with generic deterministic preflight agents and adjacent pre-execution reasoning-verification products. The current engine remains differentiated, but its strongest counterfactual is buried and its Agent OS skill packaging is not first-class.

## Frozen product invariants — MUST NOT CHANGE

- canonical thesis remains `Reasoning is not authorization.`;
- exact T0→T1 decision validity remains the product job;
- deterministic engine remains final authority;
- controlled replay remains synthetic and labeled;
- live Binance proof remains public-data-only and read-only;
- no live trade, wallet write, funding, x402 or DeFi action is added;
- no outcome/profitability/safety claim is added;
- `NO LONGER VALID` remains the hero terminal state;
- no dark crypto-dashboard redesign.

## Authorized bounded changes

### CR-01 — Surface the counterfactual already present in evidence

Judge must see simultaneously that:

- current-state market invariants still pass;
- cross-time drift relative to T0 fails;
- therefore the exact old action is blocked.

Target wording:

`CURRENT CHECKS PASS · PREMISE DRIFT FAILS`

Supporting memory line:

`A correct decision can expire.`

### CR-02 — Clarify category boundaries

Surface a category-level comparison without naming competitors:

- reasoning verifier → was the model right?
- current-state preflight → is the action acceptable now?
- permission layer → may the capability be used?
- Valid Until → is this exact previously-justified action still valid now?

### CR-03 — Package a standard portable agent skill

Add:

`skills/valid-until/SKILL.md`

Document install path and bounded agent behavior.

### CR-04 — Make Track A agent orchestration explicit

Add top-level `AGENTS.md` describing:

- agent interprets intent and proposes an exact action;
- Binance Agent OS / official Binance skill supplies observations;
- Valid Until performs deterministic validity authorization;
- BLOCK cannot be overridden by model prose;
- no execution occurs in this submission.

### CR-05 — Extend regression assurance

Update CI/runtime checks so the new signature evidence cannot silently disappear.

## Explicitly rejected changes

- local MCP server added only for breadth;
- x402;
- Agentic Wallet write path;
- live order execution;
- second LLM critic;
- risk score;
- new technical indicators;
- portfolio optimizer/rebalancer;
- redesign of the existing TRACE-approved visual language.

## Exit criteria

PBPD rework can return to TRACE only when:

1. skill package exists at standard path;
2. agent orchestration contract exists;
3. deployed hero visibly communicates cross-time distinction;
4. current checks PASS / premise drift FAIL counterfactual is truthful to `demo.json`;
5. existing tests remain green;
6. runtime browser evidence is extended to assert the new signature;
7. current category differentiation does not overclaim competitor behavior;
8. live/synthetic boundary remains explicit.

## Routing

`WINNING INTELLIGENCE → CHANGE REQUEST WI-002 → PBPD → CI/DEPLOY → TRACE GATE 6.5 DELTA RECHECK → WINNING INTELLIGENCE FINAL RECHECK → TRACE 6.75 VIDEO CONTRACT REFRESH`

No terminal readiness or submission authority is emitted by this document.
